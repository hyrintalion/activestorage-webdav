RSpec.describe ActiveStorageWebdav do
  it 'has a version number' do
    expect(ActivestorageWebdav::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(false)
  end
end
