RSpec.describe Activestorage::Webdav do
  it "has a version number" do
    expect(Activestorage::Webdav::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
